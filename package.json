{
  "name": "aceventura",
  "version": "1.0.0",
  "type": "module",
  "description": "A project to scrape AceStream data using Playwright and save it to a JSON file.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "chmod +x ./install.sh && ./install.sh"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "playwright-core": "^1.48.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SergioGMR/AceVentura.git"
  },
  "author": "SergioGMR",
  "license": "MIT",
  "eslintConfig": {
    "extends": "eslint:recommended",
    "env": {
      "node": true
    }
  }
}